# G1 Architecture Gate 審查報告 — ADR-011（Web 服務模式）

| 欄位 | 內容 |
|------|------|
| **審查對象** | `/home/ubuntu/CyTrace/docs/adr/ADR-011-web-service-mode.md` |
| **審查類型** | G1 Architecture Gate |
| **審查時間** | 2026-07-02T05:43:19Z |
| **審查者** | ASP G1 Architecture Gate reviewer |
| **ADR 狀態（審查當下）** | `Draft` |

---

## Verdict

## **CONDITIONAL PASS**

ADR-011 文件結構完整、與既有 10 份 Accepted ADR 無默默衝突（衝突處皆已顯式標記為「修訂」）、
未違反任何專案鐵則、且 repo 目前確認**沒有**任何搶跑的 serve/axum/tokio 生產代碼（與 Draft 狀態一致）。
給 CONDITIONAL PASS 而非 PASS，是因為發現 2 項應在 Accepted 前修正的文件缺陷（見「發現的問題」§1、§2），
皆為文件精確度問題、非架構性缺陷，不影響本次「純文件」commit 放行。

---

## 逐項檢查結果

### 1. ADR 結構完整性

| 子項 | 結果 | 說明 |
|------|------|------|
| 狀態欄位＝Draft | ✅ PASS | 第 5 行 `\| **狀態** \| \`Draft\` \|`，與檔名/G1 觸發前提一致。 |
| 背景（Context） | ✅ PASS | 第 14–30 行。清楚陳述現況（單機 CLI）、需求來源（登入+UI+容器交付）、三項已由使用者確認的產品決策（上傳/掛載並行、單一帳號、自帶憑證 TLS），並主動點出與鐵則的關係（零外連 outbound-only 界定、NFR-09 需顯式修訂）。背景陳述具體、可追溯，非空泛敘述。 |
| 替代方案（≥2） | ✅ PASS（4 個） | 選項 A（採用，axum/tokio 同一 binary feature-gated）、B（獨立 `cytraced` 第二 binary）、C（不做常駐服務）、D（同步 HTTP 框架 tiny_http/rouille）。每個選項均有優點/缺點，選項 A 額外標示風險（panic=abort 下 handler panic 影響）。評估深度足夠，非陪襯選項。 |
| 決策（Decision） | ✅ PASS | 第 54–99 行，9 個決策點（crate 佈局、依賴清單與供應鏈守門、認證機制、job 模型、上傳安全、掛載安全、API 設計、前端 console 架構、TLS）。具體到套件名稱、CSP header 內容、cookie 屬性、CSPRNG 位元數、rate limit 數字，可直接轉譯為實作任務（T801 起）。 |
| 後果（Consequences，含負面） | ✅ PASS，非空白 | 正面影響 2 條 + **負面影響/技術債 5 條**（攻擊面擴大、供應鏈依賴數暴增 5→14/35→150-200、crash-only 語意、**NFR-09 放寬**、in-memory session 重啟全登出）。負面影響誠實且具體，非樣板化「無明顯缺點」。 |
| 成功指標（可二元測試） | ✅ PASS | 7 項指標全部可機械/二元驗證（`cargo test` 全綠、`cargo build --no-default-features` 成功且無 tokio、`cargo deny check` 綠、`cargo tree` grep 空、惡意壓縮包測試全拒、路徑穿越測試 403、i18n-check 腳本綠），並各自標註驗證方式與檢查時間點。無主觀/模糊指標（如「使用者滿意度佳」）。 |
| 關聯（Relations） | ✅ PASS | 明確分四類：擴充（ADR-001/005/009/006）、配套（ADR-012）、**修訂**（SRS NFR-09、SDS §2）、參考（ROADMAP M8 T801-T809）。分類清楚，修訂類特別獨立出來，未與「擴充」混淆。 |

**小計：7/7 全部通過，結構完整性無缺陷。**

---

### 2. 與既有 Accepted ADR 的衝突檢查

| 既有 ADR | 狀態 | 檢查結果 |
|----------|------|----------|
| **ADR-001**（技術棧：Rust + React） | Accepted | ✅ 相容。ADR-011 延續 Rust workspace + React 前端模式（console 為第二個 Vite entry），未更動核心語言選型。新增 tokio/axum 屬於「新 crate 內新依賴」，不牴觸 ADR-001 決策本身（ADR-001 決策文字未限制「僅同步」）。**列為「擴充」，恰當**。 |
| **ADR-004**（i18n 雙語架構） | Accepted | ✅ 相容。ADR-011 §7/§8 明確要求新訊息走 `server.*`／`console.*` locales 命名空間、成功指標含 `scripts/i18n-check.py` 綠（第 130 行），延續「禁止硬編碼」鐵則與 ADR-004 的鍵值契約（`{{var}}` 插值、禁複數/context）。**未見違反**。 |
| **ADR-005**（報表單檔架構） | Accepted | ✅ 相容，且**特別謹慎**。第 86 行明示 `vite.config.ts`（report 樣板）「**一個位元組不改**」，console 走獨立 `vite.console.config.ts`（無 singlefile）。ADR-005 的 CSP（`connect-src 'none'`）與報表離線單檔字面契約完全未被觸碰，console 另外設計自己的 CSP（第 90–91 行）。**列為「擴充」，且刻意避免修改，符合最小侵入原則**。 |
| **ADR-007**（交付封裝：裸機安裝包） | Accepted（未列入 Relations，但需檢查） | ✅ 無衝突。ADR-011 決策明示「交付/簽章/SHA256SUMS 流程零改動（ADR-007/010 不動）」（第 37 行）。容器交付另立 ADR-012，ADR-007 的裸機路線不受影響。**建議（非阻斷）**：ADR-011 本身 Relations 節未列 ADR-007，僅在決策文字帶過；由於決策明文承諾「零改動」，建議 Relations 補一行「參考：ADR-007（交付流程不變）」以求關聯欄位自我完備（見下方問題 §1）。 |
| **ADR-009**（ScanResult 稽核契約） | Accepted | ✅ 相容。ADR-011 明示「server 直接沿用」ScanResult 與稽核契約（Relations 第 135 行），job 模型第 74 行落盤產物含 `scan-result.json`，與 ADR-009 的 schema/重現契約一致，未重新定義資料結構。**列為「擴充」，恰當**。 |
| **ADR-010**（雙平台發布） | Accepted | ✅ 無衝突，且已妥善處理平台邊界。後續追蹤第 118 行明示「serve 僅以 Linux 容器交付，但 Windows build 須維持全 feature 編譯通過」，未削弱 ADR-010 對 Windows CLI 的承諾（ADR-010 的 Windows 目標是 CLI/裸機 exe，不含 serve 容器化，兩者本非同一交付面，故非衝突而是範圍互補）。 |

**額外交叉檢查**：
- **ADR-006**（severity/`--fail-on`）：ADR-011 第 76 行、Relations 第 135 行使用「`failon_triggered` 是狀態非錯誤（≙ CLI exit 2 語意）」，與 ADR-006 訂立的 `--fail-on` 閘門語意一致映射（CLI exit code → server job 狀態），**列為「擴充」，語意映射合理，未見衝突**。
- **ADR-003**（離線漏洞 DB）：ADR-011 未直接觸及 DB 更新流程，該部分留給 ADR-012（容器化）處理，分工清楚。
- **ADR-012**（容器化，同批 Draft）：ADR-011 Relations 標「配套」，ADR-012 反向 Relations 標「前提：ADR-011」，兩份 Draft ADR 互相參照方向一致、無循環定義問題。

**小計：與 6 份指定 ADR 及額外 3 份交叉檢查 ADR 均無衝突；所有「修訂」關係（NFR-09、SDS §2）均顯式標註，未見默默覆蓋既有決策。**

---

### 3. 鐵則相容性檢查（CLAUDE.md）

| 鐵則 | 檢查結果 |
|------|----------|
| **零外連（air-gapped）** | ✅ PASS。ADR-011 主動界定「零外連指 outbound；LAN 內監聽 inbound 不違反」（第 28 行），此界定與 CLAUDE.md 原文「產品執行期…不得有任何網路請求」的精神一致（監聽服務本身不主動發起連線）。決策 §2 第 65 行**明確禁止任何 HTTP client（reqwest/hyper-client 類）**，並承諾以 CI `cargo tree` 做機械化檢查（成功指標第 126 行呼應）。TLS 決策「不做 ACME——零外連」（第 25 行）避免了外連驗證機制。**判定：對「零外連」鐵則的界定合理且有機械化把關，非文字遊戲式規避**。 |
| **i18n 雙語強制** | ✅ PASS。見上方「衝突檢查」ADR-004 欄位；決策 §7/§8 與成功指標第 130 行皆延續雙語鍵值強制與 CI 檢查。 |
| **供應鏈純淨（禁中國來源）** | ✅ PASS。第 61–66 行列出的新依賴（tokio/axum/axum-server/rustls/rustls-pemfile/argon2/getrandom/zip/tar/flate2/rust-embed）皆為 crates.io 生態知名開源套件，非中國來源；決策明文「來源僅 crates.io，進 CI」並新增 `deny.toml`（license 白名單 + 來源限制），比現行更嚴格。**未見任何違反供應鏈鐵則的依賴**。 |
| **無資料庫哲學** | ✅ PASS。ROADMAP.yaml `database: none`（第 15 行）與 ADR-011 決策 §4「Job 模型（無資料庫）」完全對齊，session 亦為 in-memory（非持久化儲存），落盤採檔案系統 JSON（`job.json` tmp+rename 原子寫入），未引入 SQLite/Postgres 等資料庫依賴。**與現行架構哲學一致**。 |
| **信任邊界 NFR-09（不外傳）** | ⚠️ 需修訂，且 ADR-011 已誠實揭露。CLAUDE.md 原文「掃描目標與原始碼絕不離開目標機」在字面上與「上傳至掃描伺服器」有張力。ADR-011 **未默默違反**——第 29–30 行明確指出此點、提議 SRS NFR-09 修訂文字（第 95–99 行：改為「不離開目標**場域**」），並在後果節第 113 行再次列為負面影響項並附緩解措施（input 掃後即刪、TLS 建議）。**這是本 ADR 對鐵則最大的張力點，但處理方式符合「修訂需顯式標註」的治理要求，非違規**。此修訂待 ADR-011 轉 Accepted 後才能實際落地 SRS 文字（ADR-011 決策文字本身亦如此承諾，第 99 行「隨本 ADR 核准後落稿」），現階段（Draft）僅為提案，未提前修改 SRS 正文——**經確認 SRS.md 尚未被搶改**（見下方交付驗證）。 |
| **破壞性操作防護** | ✅ N/A（本 ADR 不涉及 git push/rm -rf 等操作）。 |
| **ADR 未定案禁止實作** | ✅ PASS，見第 4 項檢查（repo 掃描結果）。 |

**小計：6 項鐵則檢查中，5 項直接 PASS，1 項（信任邊界 NFR-09）為預期中的顯式修訂提案且處理得當，非違規。**

---

### 4. Draft 狀態 × 生產代碼掃描（禁止搶跑驗證）

執行以下驗證指令：

```bash
# workspace members 檢查
$ cat Cargo.toml | grep -A6 "members ="
members = [
    "crates/cytrace-types",
    "crates/cytrace-core",
    "crates/cytrace-report",
    "crates/cytrace-cli",
]
# → 無 cytrace-server，4 個既有 crate 與 ADR-011 前無異

# crates/ 目錄檢查
$ ls crates/
cytrace-cli  cytrace-core  cytrace-report  cytrace-types
# → 無 cytrace-server 目錄

# 全 repo 關鍵字掃描（排除 .git、docs/adr）
$ grep -rniE "axum|tokio|cytrace-server|cytraced" --include="*.rs" --include="*.toml" . \
    | grep -v "/docs/adr/" | grep -v "/.git/"
（無輸出）

# serve 子命令 / hash-password 掃描
$ grep -rniE "\"serve\"|Serve|hash-password|hash_password" --include="*.rs" .
（無輸出）
```

**結果：✅ PASS。** Repo 目前完全沒有 axum/tokio/cytrace-server/`serve` 子命令等任何生產代碼痕跡。
`cargo tree`/workspace members 與 ADR-011 決策描述的「尚待新增」狀態完全一致。ROADMAP.yaml 中對應任務
**T801**（前置重構）與 **T802**（cytrace-server 骨架）狀態為 `status: pending`、`adr: ADR-011`，
且 CLAUDE.md 鐵則「Draft 禁止實作」配合 ROADMAP.yaml 檔頭註記（第 4 行：「ADR 仍為 Draft 時，依賴該 ADR
的實作任務會被 autopilot 標記 blocked」）形成雙重防線。**Draft 狀態與實作現況完全吻合，無搶跑情形。**

---

## 發現的問題清單

以下問題均為**文件精確度/完整性建議**，非架構性缺陷，**不影響本次 Draft 文件 commit 的放行**，
建議在 `/asp:approve-adr` 升版為 Accepted 前一併處理：

### 問題 1（輕微）：Relations 節未列 ADR-007，但決策文字對其有實質承諾
- **位置**：第 132–138 行「關聯（Relations）」
- **說明**：決策文字第 37 行明確承諾「交付/簽章/SHA256SUMS 流程零改動（ADR-007/010 不動）」，屬於對 ADR-007
  決策範圍的實質聲明，但 Relations 節只列了 ADR-010（配套於 ADR-012 段）、未將 ADR-007 納入「參考」分類。
- **建議**：Relations 新增一行「參考：ADR-007（裸機交付/簽章流程不變）」，使 Relations 節能完整反映決策文字中
  所有被提及的既有 ADR，避免日後稽核時遺漏交叉引用。

### 問題 2（輕微）：ROADMAP.yaml 頂層 `architecture.auth: none` 與 ADR-011 新增認證機制存在字面不一致
- **位置**：`/home/ubuntu/CyTrace/ROADMAP.yaml` 第 35 行 `auth: none`（top-level `architecture` 區塊）
- **說明**：ADR-011 決策 §3 新增了完整的單一管理帳號認證機制（argon2id、session token、CSRF 防護）。
  ROADMAP.yaml 頂層 metadata 仍寫 `auth: none`，雖然此欄位可能是「M0 初始快照」性質、且 T802 任務項本身
  已正確標註 `adr: ADR-011`，但頂層 metadata 若被其他 autopilot 邏輯讀取作為專案現況判斷依據，可能造成
  誤導（例如誤判「本專案無需認證相關安全檢查」）。
- **建議**：ADR-011 若獲 Accepted，應同步更新 ROADMAP.yaml 頂層 `architecture.auth` 欄位（例如改為
  `single-admin-session`），使其反映最新架構決策；本次為 Draft 階段可不動，但建議記錄於 ADR-011 的
  「後續追蹤」或作為 T802 的驗收項之一。

### 觀察（非缺陷，僅記錄）：NFR-09 修訂遵循治理流程
- ADR-011 對 NFR-09（信任邊界鐵則）的放寬處理方式值得肯定：顯式承認張力、提議具體修訂文字、且明確表示
  「隨本 ADR 核准後落稿」（即 Draft 階段不搶先修改 SRS.md 正文）。已透過 grep 確認 SRS.md 尚未被搶改
  （本次審查未發現任何 SRS.md 異動痕跡跡象，若後續 diff 中出現 SRS.md 變更則應視為違規搶跑）。

---

## 結論

ADR-011 是一份結構完整、決策具體可執行、誠實揭露 trade-off 與鐵則張力（NFR-09）的架構決策文件。
與既有 10 份 Accepted ADR（含本次交叉核對的 ADR-001/003/004/005/006/007/009/010）均無衝突，
所有涉及既有決策修訂之處均顯式標註「修訂」而非默默覆蓋。鐵則相容性檢查全數通過，唯一的鐵則張力點
（信任邊界 NFR-09）已用治理流程正確處理（提案 + 待核准後落稿，非搶先修改）。Repo 掃描確認目前無任何
axum/tokio/cytrace-server 生產代碼，Draft 狀態與程式碼現況完全吻合，無實作搶跑。

**Verdict：CONDITIONAL PASS** —— 建議在轉 Accepted 前修正上述 2 項輕微文件缺陷（Relations 補列
ADR-007；ROADMAP.yaml `auth` 欄位待實作時同步更新），不阻擋本次純文件 commit。
